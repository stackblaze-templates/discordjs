services:
  - type: web
    name: discordjs-bot
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: node index.js
    autoDeploy: false
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: CLIENT_ID
        sync: false
